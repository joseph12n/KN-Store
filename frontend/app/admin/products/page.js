import ProductsCrudPanel from '@/components/ProductsCrudPanel';

export const metadata = {
  title: 'Productos — Admin KN Store',
};

export default function ProductsPage() {
  return <ProductsCrudPanel />;
}
