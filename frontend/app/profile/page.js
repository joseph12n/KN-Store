import MyProfilePanel from '@/components/MyProfilePanel';

export const metadata = {
  title: 'Mi Cuenta — KN Store',
  description: 'Gestiona tu perfil en KN Store.',
};

export default function ProfilePage() {
  return <MyProfilePanel />;
}
